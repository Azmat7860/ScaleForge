# Database Folder

Use this folder for database-level concerns:

- mongo seed scripts
- fixtures and sample datasets
- reusable query helpers
- indexes or data migration scripts when your project grows

Suggested next step:

Add Mongoose schemas/models and move your real queries from repositories into MongoDB.
